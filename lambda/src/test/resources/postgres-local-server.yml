version: '2'

services:
  postgres:
    image: 'postgres:10.3-alpine'
    ports:
      - "5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mealfu

